

// todo faire en sorte que les actions sur les objets retournent une description de ce qui ce passe, plutôt qu'un print

// todo rendre les attributs du model accessible à la vue

// todo faire un générateur de commandes avec le controller

// todo se renseigner pour pouvoir afficher une image différente à chaque salle

// todo faire en sorte qu'on puisse prendre les golds des coffres

// todo réparer la map et les mobs qui ne meurent pas

// todo réparer le leveling up (mauvais nbr de kills)

// todo réparer le leveling up (mauvais nbr de pv gagnés)

// todo trouver où mettre le bouton 'use'

// todo faire en sorte de pouvoir utiliser les clés sur les portes. Actuellement on ne peut pas select une porte !

// todo gérer equip unequip

// todo gérer le bouton 'Talk' qui se transforme en 'Loot' lorsqu' on a un coffre de sélectionné



on n'a plus droit qu'à 3 containers par salle par soucis de display


On a en tout : h = 768, l = 1024 et 24 de padding de chaque côté du root